language: python
python:
  - 2.7
addons:
  postgresql: "9.3"
before_install:
  - export TZ=Canada/Pacific
install:
  - pip install -i http://tools.pacificclimate.org/pypiserver/ -r requirements.txt -r test_requirements.txt
  - pip install .
script: py.test -v --tb=short tests
